spring:
  application:
    name: gateway

  redis:
    host: localhost
    port: 6379

eureka:
#  instance:
#    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka


server:
  port: 8072